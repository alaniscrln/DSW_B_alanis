{% extends 'base.html.twig' %}

{% block title %}{{parent()}}{% endblock %}

{% block body %}
<div class="container h-100">
    <div class="row h-100 justify-content-center align-items-center">
        <div class="col-6 bg-light rounded my-3 p-5">
            <h1>Crear cuenta</h1>
            {{ form(formulario) }}
        <button class="btn btn-secondary" type="submit" onclick="window.location.href= '{{path('app_login')}}' ">
            Ya tengo cuenta
        </button>
        </div>
    </div>
</div>

{% endblock %}
